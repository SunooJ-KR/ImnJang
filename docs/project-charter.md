# 과제계획서 — 임앤장 (ImnJang)

> **한 줄 정의**: 관심 있는 아파트를 직접 담아서 같은 기준으로 나란히 비교하는 서비스
>
> ⚠️ 제품 정의의 정본은 [`product-identity.md`](product-identity.md)다 (2026-09-11 재정립).

| 항목 | 내용 |
|---|---|
| 과제명 | 임앤장 (ImnJang) — 물리 지표 기반 아파트 탐색 서비스 |
| 대회 | 원티드 AI Championship 2026 (팀 참가) |
| 과제 기간 | 2026-09-09 ~ 2026-09-20 (12일) |
| 제출 마감 | **2026-09-20** |
| 작성일 | 2026-09-10 |
| 관련 문서 | [`plan.md`](../plan.md) 실행 계획 · [`docs/algorithms.md`](algorithms.md) 알고리즘 · [`docs/decisions.md`](decisions.md) 결정 이력 |

---

## 1. 추진 배경 및 필요성

아파트를 고를 때 일조와 조망은 실거주 만족도를 크게 좌우하지만, 현재 어떤 서비스도 이를 **탐색 조건**으로 쓰지 못한다.

호갱노노·직방·더스택은 이미 3D 단지투어로 계절별 일조량과 세대별 조망을 제공한다. 따라서 일조·조망 시뮬레이션 자체는 차별점이 아니라 **필수 기능**이다. 문제는 사용 방식에 있다 — 이들 서비스는 단지를 하나씩 열어 눈으로 **확인(inspection)** 하게 만든다. 서울 9,000개 단지를 그렇게 볼 수는 없다.

즉 시장에 없는 것은 시뮬레이션이 아니라 **시뮬레이션 결과로 전체를 정렬하는 탐색(search)** 이다.

## 2. 과제 목표

서울 전역 아파트를 대상으로 일조·조망·접근성을 사전 계산하고, 사용자가 조건을 걸면 **서울 전체가 즉시 정렬되는** 반응형 웹 서비스를 구축한다.

부차 목표로, 물리 시뮬레이션과 지도학습을 결합해 **런타임 생성형 AI 없이도 검증 가능한 AI 서비스**가 성립함을 보인다.

## 3. 과제 범위 (확정 제약)

| # | 항목 | 확정 내용 |
|---|---|---|
| 1 | 런타임 생성형 AI | **전면 금지.** 서비스 중 LLM 호출 0회 (개발 도구로서의 AI 에이전트는 자유) |
| 2 | 대상 | 서울 전역 아파트 단지 |
| 3 | 거래 유형 | 매매 + 전세 (월세 전용 단지 제외) |
| 4 | 폼팩터 | 반응형 웹앱, 모바일 우선 |
| 5 | 핵심 동선 | 지도 탐색 → 단지 클릭 → 리포트 |
| 6 | 타깃 | 실거주자 코어, 투자자는 확장 영역 |
| 7 | 비용 | 무료 API·무료 티어만 사용. 운영비 0원 |

**범위 밖**: 서울 외 지역, 오피스텔·빌라, 세대 단위(호수·향) 예측, 중개·거래 기능.

세대 단위 예측을 범위에서 제외한 이유는 공공 실거래 API가 층까지만 제공하고 향과 호수가 없어, 같은 동·같은 층에 남향과 북향이 공존하는 것을 구분할 수 없기 때문이다. 예측 단위는 **단지 × 면적타입 × 층대**로 확정했다.

## 4. 차별화 전략

**① 확인이 아니라 탐색**
조건을 걸면 서울 전체가 즉시 정렬된다. 발표 첫 30초를 필터링 데모로 구성해 이 차이를 시각적으로 드러낸다.

**② 환각 없는 검증 가능한 AI**
물리 시뮬레이션(horizon 엔진) + LightGBM 조합. 모든 수치가 재현 가능하고 근거를 역추적할 수 있다. 생성형 AI의 환각 위험이 구조적으로 존재하지 않는다.

**SHAP는 쓰지 않는다.** 입지 feature끼리 상관이 높아 attribution이 배경 설정에 좌우되고 인과를 뜻하지 않는다.

**측정 결과 일조·조망은 가격을 예측하지 못했다.** 지역 통제 후 bootstrap CI가 0을 포함한다(`product-identity.md` §5.3). 이를 숨기지 않고 "측정해서 아니라고 말할 수 있는 것"을 차별점으로 삼는다.

**③ 모르는 것을 모른다고 표기**
데이터가 없거나 신뢰도가 낮은 항목은 보간하지 않고 결측으로 남긴다. 매칭 실패 단지는 정렬에서 제외하고 회색 마커로 사유를 표기한다.

## 5. 시스템 구성

### 5.1 레이어 (단계별 완결 원칙)

각 레이어는 독립적으로 데모 가능해야 한다. 상위가 미완이어도 하위까지로 출품한다.

```
Layer 3  전세가율 · 정비사업 · 단지 비교              [여력 시]
   ▲
Layer 2  단지 시세 모델 (LightGBM)                   [목표선]
   ▲
Layer 1  ★ 지도 필터링·정렬 + horizon 지표  ← 차별점의 실체. 자원 집중
   ▲
Layer 0  데이터 수집 + 단지/건물 마스터
```

### 5.2 저장소 구조

```
data/     배치 파이프라인 — collect(수집) / master(마스터) / verify(검증) / archive(프로토타입)
models/   horizon 엔진 · 시세 모델
front/    Next.js 웹앱
docs/     문서 · mockups
output/   산출물 (커밋 제외)
```

분리 기준은 담당 역할이다 (§7). 스크립트 번호 접두사는 실행 순서이며 폴더를 가로질러 이어진다.

### 5.3 파이프라인

```
[배치 · Python · 로컬]
  공공데이터 수집 (실거래 API + OSM Overpass)
    → 단지/동 마스터 (지오코딩 + 공간 매칭)
    → horizon 엔진   ← 단일 연산에서 일조·개방도·향별값 전부 파생
    → 부가 지표 (도로근접, 표고, 생활 인프라)
    → 단지 시세 모델 → 적재

[런타임 · Next.js on Vercel]
  지도(카카오맵) → 필터·정렬 → 단지 클릭 → 리포트
  ※ 런타임 연산 없음. 전부 사전 계산 조회
```

**원칙: 런타임은 조회만 한다.** 데모 중 연산 지연·실패의 여지를 없앤다.

### 5.4 기술 스택

| 영역 | 선택 |
|---|---|
| 프론트 | Next.js 15 + TypeScript + Tailwind + shadcn/ui |
| 지도 | 카카오맵 JS SDK (무료 30만/일) |
| 배포 | Vercel 무료 티어 |
| DB | Supabase (Postgres + PostGIS) 무료 티어 |
| 배치 | Python 3.11, geopandas · shapely · pyproj · numpy |
| 태양 위치 | pvlib |
| ML | LightGBM (6종 비교 후 채택) · statsmodels |

**좌표계 규칙**: 저장·표시는 WGS84(EPSG:4326), 거리·면적 연산은 EPSG:5179(UTM-K). 변환 누락은 반복 발생하는 버그이므로 유틸 하나로 강제하고 assert로 검증한다.

## 6. 추진 일정

| Day | 날짜 | 작업 | 검토 항목 |
|---|---|---|---|
| D1 | 09-09 (화) | ✅ 데이터 소스 검증 · horizon 프로토타입 · API 키 확보 | 완료 |
| D2 | 09-10 (수) | ✅ 서울 25개 구 전체 수집 · 셀 커버리지 측정 | 완료 (예측 단위 확정) |
| **D3** | **09-11 (목)** | **마스터 구축 (최대 난관)** — 지오코딩 → 동 배정 → 층화 100개 수동 검수 | **매칭률 확인 / 선제 절단 발동 여부** |
| D4 | 09-12 (금) | ✅ horizon 배치 + 접근성 지표 | 완료 |
| D5 | 09-13 (토) | ✅ `complex_metrics` 3테이블 완성 | 완료 |
| D6 | 09-14 (일) | **웹앱 S1 지도+필터** (차별점의 실체. ML보다 우선) | **화면 검토** |
| D7 | 09-15 (월) | S2 단지 리포트 + S3 근거 상세 + 조건 완화 추천 | 화면 검토 |
| D8 | 09-16 (화) | **Vercel 배포 + Supabase + 카카오 도메인 등록** | **공개 URL 확보** |
| D9 | 09-17 (수) | 단지 시세 모델 (Layer 2). 시간 부족 시 생략 | 모델 채택/폐기 |
| D10 | 09-18 (목) | 데모 시나리오 · 버그 수정 · 정적 fallback | 리허설 |
| D11 | 09-19~20 | 제출물 작성 · **제출** | 최종 검토 |

**병목은 코딩이 아니라 검토·의사결정 시간이다.** 구현은 AI 에이전트가 수행하므로, 일정 리스크는 사람이 판정해야 하는 지점(D3 수동 검수, D6 화면 검토)에 집중된다.

## 7. 역할 분담

| 역할 | 담당 | 디렉토리 | 책임 범위 |
|---|---|---|---|
| 웹앱 프론트 | **정선우** | `front/` | S1~S4 화면, 카카오맵 연동, 반응형 (D6~D7) |
| 모델 개발 | **김용진 · 서지은** | `models/` | horizon 엔진 서울 전역 배치, 부가 지표, 단지 시세 모델 (D4~D5, D9) |
| 데이터 파이프라인 | (미정) | `data/` | 수집·지오코딩·동 배정·마스터 유지 (D1~D3 완료, 이후 유지보수) |
| 검증 / 품질 | (미정) | `data/verify/` | 층화 정답셋 수동 검수, 표본 육안 검증, 지표 검토 |
| 배포 / 인프라 | (미정) | — | Vercel·Supabase·카카오 도메인 등록, 정적 fallback (D8) |
| 발표 / 제출물 | (미정) | `docs/` | 데모 시나리오, 발표 자료, 제출 문서 (D10~D11) |
| 총괄 / 의사결정 | (미정) | — | 판단 게이트 발동, 선제 절단 결정, 최종 제출 |

디렉토리 분리가 곧 역할 분리다. 서로 다른 폴더를 만지므로 충돌이 나지 않는다.

**공동 책임 사항**
- 결정 사항은 반드시 [`docs/decisions.md`](decisions.md)에 근거와 함께 기록한다. 게이트 판정 수치는 로그가 아니라 문서에 남긴다 (`output/*.log`는 커밋되지 않는다).
- `data/`가 만든 `output/15.1.complex_final.txt`가 `models/`의 입력이고, `models/`가 만든 지표가 `front/`의 입력이다. 스키마를 바꿀 때는 다음 단계 담당에게 먼저 알린다.
- 검수용 `output/16.2.audit_sample.xlsx`는 바이너리라 git이 병합하지 못한다. 여러 명이 나눠 검수하려면 파일을 층별로 분리해야 한다.

## 8. 산출물

| 구분 | 산출물 | 상태 |
|---|---|---|
| 서비스 | 공개 URL (Vercel 배포 반응형 웹앱) | D8 |
| 데이터 | 단지 마스터 (`output/15.1.complex_final.txt`) | ✅ D3 |
| 데이터 | 단지별 물리 지표 테이블 (`output/23.2.complex_metrics.txt`) | ✅ D5 |
| 문서 | [프론트엔드 데이터 계약](data-contract.md) | ✅ D5 |
| 모델 | cold-start 시세 추정 + 예측구간 + 비교사례 | D2 완료 |
| 문서 | 실행 계획 · 알고리즘 명세 · 결정 이력 | 진행 중 |
| 제출물 | 발표 자료 · 데모 시나리오 | D11 |

## 9. 리스크 및 대응

| # | 리스크 | 영향 | 확률 | 대응 |
|---|---|---|---|---|
| **R2** | **단지↔동 매칭 실패** | **높음** | **중상** | 좌표 매칭 → 경계 폴리곤 매칭으로 전환. 실패 단지는 정렬에서 제외 + 회색 마커로 사유 표기. LOW 신뢰도는 정렬 근거로 쓰지 않음 |
| R2b | D3 지연 → 전 일정 연쇄 | 높음 | 중상 | §10.2 선제 절단 즉시 발동 |
| R6 | "일조·조망은 이미 있다" 지적 | **높음** | **높음** | §4 탐색 vs 확인 프레임. 발표 첫 30초를 필터링 데모로 |
| R7 | "생성형 AI 없으면 AI냐" 지적 | 중 | 낮음 | 대회 요건에 의무 없음. cold-start MAPE 70.3%→19.5%와 "일조는 가격을 예측하지 않는다"는 직접 측정으로 답한다 |
| R8 | 카카오맵 도메인 미등록으로 프로덕션 지도 실패 | 중 | 중 | D8 배포 체크리스트에 포함 |
| R12 | 실거래 API 일 호출 한도 | 중 | 중 | 학습 데이터는 `rt.molit.go.kr` CSV 벌크 병행 |
| R13 | 교통량 관측 지점 결측 | 낮음 | 높음 | 300m 내 지점 없으면 결측 처리 + "관측 지점 없음" 표기. 보간하지 않음 |
| ~~R1~~ | ~~horizon 배치 시간 초과~~ | — | — | **D1 해소** (서울 전역 6분) |
| ~~R3~~ | ~~GIS건물통합정보 라이선스~~ | — | — | **D1 해소** (OSM 전환) |
| ~~R11~~ | ~~학습 셀 커버리지 부족~~ | — | — | **D2 해소** (거래 점유율 82.3%) |

## 10. 판단 게이트

협상하지 않는다. 조건이 걸리면 즉시 발동한다.

### 10.1 강제 게이트

- **D5 종료 시 `complex_metrics` 없으면** → 핵심 5개 구로 축소. 단, 조용히 줄이지 않고 앱·제출물에 "검색 가능 범위: ○○구"를 명시
- **D8 종료 시 배포 URL 없으면** → 기능을 잘라내서라도 배포. **미배포는 실패**
- **D9 시작 시 D8 미완이면** → ML 전면 생략, Layer 1로 확정 출품

### 10.2 D3 지연 시 선제 절단 (순서대로)

| 순서 | 절단 대상 |
|---|---|
| 1 | `river_view` / `park_view` / `mountain_view` |
| 2 | 봄가을 일조 (겨울만으로 충분) |
| 3 | 생활 인프라 우선순위 4~6 |
| 4 | S4 가중치 슬라이더 |
| 5 | Layer 2 ML 전체 |
| 6 | 조건 완화 추천 |

**끝까지 지키는 최소 집합**: 겨울 일조 · 조망 개방도 · 도로/철도 거리 · 추정 역 접근성 · S1 전역 필터링 · D8 배포.

## 11. 검증 계획

| 대상 | 방법 | 기준 | 상태 |
|---|---|---|---|
| 자기 건물 제외 | 단독 배치 동의 일조 계산 | 거의 전 시간 확보 | ✅ D1 |
| 높이 datum | 경사지 아래/위 동 차폐 비교 | 위쪽 동이 더 높게 계산 | ✅ D1 |
| near-field 해상도 | 인접 동 10~20m 관측점 | 방위 bin 연속 차폐 | ✅ D1 |
| 층↑ → 일조↑ | 층대별 중앙값 | 단조 증가 | ✅ D1 |
| 단지 매칭 | 층화 정답셋 100개 (HIGH 30 / MIXED 30 / LOW 40) | HIGH 무오류, MIXED 90%+ | **D3 진행 중** |
| 조망 | 한강변 단지 `river_view` | True | D4 |
| 민감도 | 관측 높이 ±3m, 반경 ±500m | 순위 불변 | D4 |
| 시세 모델 | 시간 split + 단지 홀드아웃 | 물리 feature 유무 차이 보고 | D9 |
| 웹앱 | 실기기 전 화면 | 가로 스크롤 없음, 3초 내 로드 | D8 |

각 배치 스크립트는 assert 기반 자체 검증을 포함한다. 별도 테스트 프레임워크는 도입하지 않는다.

## 12. 현재 진행 상황 (2026-09-10 기준)

| 단계 | 결과 |
|---|---|
| D1 | horizon 엔진 2.9ms/관측점 (서울 전역 6분), OSM만으로 아파트 동 높이 95.0% 확보 |
| D2 | 매매 247,362행 / 전월세 1,306,717행 / OSM 건물 70,250동 / POI 63,390건 수집 완료 |
| D3 | 지오코딩 9,160/9,164 (**99.96%**). 동 배정은 단지 경계 폴리곤 + 한국부동산원 등록 동수 상한으로 재설계 |

**D3 결과**: 한국부동산원 공동주택 단지 식별정보(서울 아파트 9,458건)를 지번주소로 조인해 정답 동수를 확보하고 배정 상한으로 걸었다. 동수 정확 일치율이 60.8%에서 **93.2%**로 올랐고 과다 배정은 1,760건에서 **0건**이 됐다. 준공년도를 등록 정보 기준으로 재측정하니 **99.9%** — 지오코딩과 단지 식별 자체는 사실상 완벽하며, D3의 문제는 처음부터 끝까지 동 배정 하나였다.

**남은 게이트 미달**: 동을 1개 이상 받은 단지가 72.7%로 기준 80%에 미달한다(거래량 가중으로는 96.8%). 원인은 알고리즘이 아니라 OSM에 해당 동이 없거나 `building=residential`로 태깅된 데이터 커버리지다.

**검증 주의**: 등록 동수를 배정 상한으로 쓰는 한, 등록 동수로 하는 검증은 과소만 잴 수 있고 "맞는 동을 골랐는가"는 재지 못한다. 그것은 층화 100건 수동 검수로만 확인된다 (§11).
