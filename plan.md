# 임앤장 (ImnJang) — 실행 계획

> **한 줄 정의**: 서울 아파트 4,000개를 "살기 좋은 순서"로 정렬해주는 지도

- 대회: 원티드 AI Championship 2026 · **팀 참가** (역할 분담은 [`docs/project-charter.md`](docs/project-charter.md) §7)
- 문서 버전: v4 (D1 실측 반영)
- 최종 수정: 2026-09-09

## 문서 구성

| 문서 | 내용 |
|---|---|
| **`plan.md`** (이 문서) | 실행 계획 — 제약, 아키텍처, 일정, 리스크, 판단 게이트 |
| [`docs/algorithms.md`](docs/algorithms.md) | 알고리즘 상세 (horizon 엔진, 시세 모델, 조건 완화, 생활 인프라) |
| [`docs/data-sources.md`](docs/data-sources.md) | 데이터 소스 목록 + **D1 실측 검증 결과** |
| [`docs/data-model-and-ui.md`](docs/data-model-and-ui.md) | DB 스키마, 화면 설계 |
| [`docs/decisions.md`](docs/decisions.md) | v1→v4 변경 이력, 외부 리뷰 대응 |
| [`docs/pitch-and-roadmap.md`](docs/pitch-and-roadmap.md) | 확장 로드맵, 심사·발표 대응 |

---

## 1. 데드라인

| 항목 | 날짜 |
|---|---|
| 참가 신청 마감 | **09-18** |
| **프로젝트 제출 마감** | **09-20** |
| 예선 심사 + 온라인 투표 | 09-21 ~ 10-05 |
| 본선 20팀 발표 | 10-07 |
| 데모데이 | 10-17 |

예선 = 심사위원 80% + 온라인 투표 20%. 기준: 기획력 / 실현 가능성 / 확장성 / **AI 활용 적절성**.

**가용: 09-09 ~ 09-20, 하루 2~4시간.** 코딩은 AI 에이전트가 하므로 병목은 **사용자의 검토·의사결정 시간**이다.

---

## 2. 확정 제약

| # | 항목 | 확정 |
|---|---|---|
| 1 | 런타임 생성형 AI | **전면 금지.** 서비스 중 LLM 호출 0회. 개발용 에이전트는 자유 |
| 2 | 대상 | 서울 전역 아파트 단지 (약 4,000개) |
| 3 | 거래 유형 | 매매 + 전세 |
| 4 | 폼팩터 | 반응형 웹앱 (모바일 우선) |
| 5 | 핵심 동선 | 지도 탐색 → 단지 클릭 → 리포트 |
| 6 | 타깃 | 실거주자 코어, 투자자는 확장 |
| 7 | 비용 | 무료 API / 무료 티어. 운영비 0 |

---

## 3. 차별화 (요약)

경쟁사(호갱노노·직방·더스택)는 **이미 3D 일조·조망을 제공한다.** 이것은 차별점이 아니라 **필수 기능**이므로 포함하되, 여기서 우위를 주장하지 않는다.

실제 차별점은 둘이다.

**① 확인(inspection)이 아니라 탐색(search)**
기존 서비스는 단지를 하나씩 열어 눈으로 본다. 서울 4,000개를 그렇게 볼 수는 없다.
임앤장은 조건을 걸면 서울 전체가 즉시 정렬된다.

```
"겨울 일조 5시간 이상 + 조망 트임 상위 30% + 추정 도보 10분 + 매매 10억 이하"
                          ↓
                  조건 만족 27개 단지
```

이것은 **모든 단지 지표를 사전 계산해야만** 성립한다. 그래서 서울 전역 커버리지를 축소하지 않는다.

**② 계산 근거의 완전 공개**
경쟁사의 3D 일조는 블랙박스다. 임앤장은 계산식·가정·데이터 출처·**알 수 없는 것**을 전부 공개한다.

> 상세: [`docs/pitch-and-roadmap.md`](docs/pitch-and-roadmap.md), 정직한 한계 표기는 [`docs/data-model-and-ui.md`](docs/data-model-and-ui.md) S2 화면

---

## 4. 아키텍처

### 4.1 레이어 (단계별 완결 원칙)

각 레이어는 **독립적으로 데모 가능**해야 한다. 상위가 미완이어도 하위까지로 출품한다.

```
Layer 3  전세가율 · 정비사업 · 단지 비교              [여력 시]
   ▲
Layer 2  단지 시세 모델 (LightGBM)                   [목표선]
   ▲
Layer 1  ★ 지도 필터링·정렬 + horizon 지표  ← 차별점의 실체. 자원 집중
   ▲
Layer 0  데이터 수집 + 단지/건물 마스터
```

### 4.2 파이프라인

```
[배치 · Python · 로컬]
  공공데이터 수집 (실거래 API + OSM Overpass)
    → 단지/동 마스터 (지오코딩 + 공간 매칭)
    → horizon 엔진   ← 단일 연산에서 일조·개방도·향별값 전부 파생
    → 부가 지표 (도로근접, 표고, 생활 인프라)
    → 단지 시세 모델
    → 적재

[런타임 · Next.js on Vercel]
  지도(카카오맵) → 필터·정렬 → 단지 클릭 → 리포트
  ※ 런타임 연산 없음. 전부 사전 계산 조회.
```

**원칙: 런타임은 조회만 한다.** 데모 중 연산 지연·실패의 여지를 없앤다.

---

## 5. 기술 스택

| 영역 | 선택 |
|---|---|
| 프론트 | Next.js 15 + TypeScript + Tailwind + shadcn/ui |
| 지도 | 카카오맵 JS SDK (무료 30만/일) |
| 차트 | Recharts |
| 배포 | Vercel 무료 |
| DB | Supabase (Postgres + PostGIS) 무료 |
| 배치 | Python 3.11 (`.venv`) |
| 공간 | geopandas, shapely, pyproj, **numpy** (horizon 엔진 핵심) |
| 태양 | pvlib |
| ML | LightGBM + SHAP |

**좌표계**: 저장·표시 WGS84(EPSG:4326), 거리·면적 연산 **EPSG:5179(UTM-K)**. 변환 누락은 흔한 버그이므로 유틸 하나로 강제하고 assert로 검증한다.

**운영 준비**: 카카오 앱의 **카카오맵 활성화 ON + 배포 도메인 등록** (미등록 시 프로덕션에서 지도가 안 뜬다), Vercel 환경변수, Supabase 장애 대비 **정적 fallback 데이터**(데모 단지 3곳).

---

## 6. D1 실측 결과 (09-09 완료)

> 전문: [`docs/data-sources.md`](docs/data-sources.md)

| 항목 | 결과 |
|---|---|
| **horizon 엔진** | ✅ 검증 4/4 통과. **2.9ms/관측점 → 서울 전역 12만 점 약 6분** |
| **OSM 건물 폴리곤** | ✅ 아파트 동 높이 **95.0%** 확보. **GIS건물통합정보(CC BY-NC-ND) 의존 제거** → 라이선스 리스크 소멸 |
| **실거래 매매 API** | ✅ **상세 엔드포인트에만 `aptSeq` 존재** (100%). 일반 엔드포인트는 쓰지 않는다 |
| **실거래 전월세 API** | ✅ `aptSeq` 100%, 전세 비율 44%. **거래량이 매매의 약 9배** |
| `aptDong` | 26%만 확보 → **세대 단위 모델 폐기가 옳았음이 실증됨** |
| **사슬 연결 (단지↔동)** | ⚠️ 문자열 매칭 32.5% (목표 60%) → **좌표 매칭으로 전환**, 재측정 중 |

### 계획 수정 사항

| 이전 | D1 이후 |
|---|---|
| 건물 폴리곤 = GIS건물통합정보 | **OSM (ODbL)** |
| 건물 높이 = 층수 × 2.8m | **`height` 태그 우선**, 없으면 층수 × 2.8m |
| 배치 6시간 예상, 야간 실행 | **6분.** 야간 배치 불필요 |
| 1km 반경은 일조에 영향 없음(554m) | **오류.** 09시 고도 11.6° → **206m**. 150m 이상 초고층을 거리 무관 전역 후보로 포함 |
| 단지 매칭 = 폴리곤 포함 판정 | **지오코딩 좌표 → 동 클러스터 공간 매칭** |
| 전월세 저장 | 행 수 과다(5년 서울 100만+) → **집계 저장**, 원본은 로컬 |

**주의**: 매매 API는 `roadNm`, 전월세 API는 `roadnm` — **필드명 대소문자가 다르다.** 정규화 계층 없이 공통 처리하면 조용히 결측이 된다.

---

## 6b. D2 실측 결과 (09-10 완료)

| 항목 | 결과 |
|---|---|
| **매매** | 247,362행 / 7,883단지. `aptSeq` 결측 **0.00%** |
| **전월세** | 1,306,717행 / 9,373단지. 전세 비율 **56.1%**, 매매의 **5.3배** |
| **OSM 건물** | 70,250동 (아파트 **37,269동**, 150m 이상 140동), 단지 경계 4,285 |
| **아파트 높이** | 87.8% (`height` 태그 25,815 / `levels` 6,913 / **결측 4,541**) — D1 강남 95.0%보다 낮다 |
| **POI** | 7종 63,390건 (편의점 7,012 · 음식점 46,414 · 지하철 2,451 · 학교 2,135 · 공원 3,042 · 마트 1,709 · 병원 627) |
| **셀 커버리지** | 셀 35,385개. 5건+ 셀 36.9%가 **전체 거래의 82.3%**를 담는다 |

### D2 확정 사항

| 항목 | 확정 |
|---|---|
| **면적타입** | **3m 폭 bin** (`round(전용면적/3)×3`). 문서에 정의가 없던 값을 실측으로 확정 — 1m 반올림은 84.5978/84.9984를 갈라놓고, 5m는 다른 평형을 섞는다 |
| **예측 단위** | **단지 × 면적타입 × 층대 유지.** floor_band 제거의 실익은 학습가능 셀 +9.8%p뿐인데, 층대는 일조·조망이 갈리는 축이라 빼면 물리 지표와 시세의 연결이 끊긴다 |
| **거래 5건 미만 셀** | 예측은 하되 **신뢰도 LOW 표기.** 정렬 근거로 쓰지 않는다 (R2와 동일 원칙) |
| **높이 결측 4,541동** | D3 매칭 후 **같은 단지 다른 동의 높이 중앙값으로 대체**, 대체 여부를 플래그로 남겨 리포트에 공개. 단지 매칭 실패로 대체 불가한 동만 제외 |

**주의**: `requests` 예외 메시지에는 요청 URL이 통째로 들어간다. 실패 로그를 파일로 남기면 **serviceKey가 평문으로 새므로** 반드시 마스킹한다 (`data/collect/11.collect_trades.py`의 `mask_key`).

---

## 7. 일정

| Day | 날짜 | 작업 | 사용자 결정 |
|---|---|---|---|
| **D1** | 09-09 (화) | ✅ 참가 신청 · 데이터 소스 검증 · horizon 프로토타입 · API 키 확보 | 완료 |
| **D2** | 09-10 (수) | ✅ 서울 25개 구 전체 수집 — 실거래 5년치, OSM 전역, 생활 인프라 POI + 셀 커버리지 측정 | 완료 (예측 단위 확정) |
| **D3** | 09-11 (목) | **마스터 구축 (최대 난관)** — 지오코딩 → 클러스터 매칭 → **층화 100개 수동 검수** | **매칭률 확인 / §8.2 절단 발동 여부** |
| **D4** | 09-12 (금) | horizon 엔진 서울 전역 배치 + 부가 지표 | 표본 10동 눈으로 검증 |
| **D5** | 09-13 (토) | `complex_metrics` 완성 + 생활 인프라 조인 | 지표 검토 |
| **D6** | 09-14 (일) | **웹앱 S1 지도+필터 (차별점의 실체. ML보다 우선)** | **화면 검토** |
| **D7** | 09-15 (월) | S2 단지 리포트 + S3 근거 상세 + 조건 완화 추천 | 화면 검토 |
| **D8** | 09-16 (화) | **Vercel 배포 + Supabase + 카카오 도메인 등록 + 모바일** | **공개 URL 확보** |
| **D9** | 09-17 (수) | 단지 시세 모델 (Layer 2). 시간 부족 시 **생략** | 모델 채택/폐기 |
| **D10** | 09-18 (목) | 데모 시나리오 · 버그 수정 · 정적 fallback | 리허설 |
| **D11** | 09-19~20 | 제출물 작성 · **제출** | 최종 검토 |

---

## 8. 판단 게이트

협상하지 않는다. 조건이 걸리면 즉시 발동한다.

### 8.1 강제 게이트

> **커버리지 현황 (D4 종료)**: 동을 배정받은 단지 71.0%, 지표 산출 단지 6,450개.
> 게이트 80% 미달이나 `complex_metrics`는 존재하므로 아래 "5개 구 축소" 조건은 발동하지 않는다.
> 거래량 가중 96.0%는 보조 KPI일 뿐 게이트 대체가 아니다 (`docs/decisions.md` 39).
> 앱은 미배정 단지를 숨기지 말고 `분석 불가 — 건물 footprint 미매칭`으로 표기한다.

- **D5 종료 시 `complex_metrics` 없으면** → 핵심 5개 구로 축소. 단, **조용히 줄이지 않고** 앱·제출물에 "검색 가능 범위: ○○구"를 명시
- **D8 종료 시 배포 URL 없으면** → 기능을 잘라내서라도 배포. **미배포는 실패**
- **D9 시작 시 D8 미완이면** → ML 전면 생략, Layer 1로 확정 출품

### 8.2 D3 지연 시 선제 절단 (순서대로)

D3(매칭)가 **가장 먼저 밀릴 날**이다. 밀리면 D4 배치 입력이 흔들려 전 일정이 연쇄 지연된다.

| 순서 | 절단 대상 |
|---|---|
| 1 | `river_view` / `park_view` / `mountain_view` (별도 LOS 연산) |
| 2 | 봄가을 일조 (겨울만으로 충분) |
| 3 | 생활 인프라 우선순위 4~6 (야간상권·교통량·생활인구) |
| 4 | S4 가중치 슬라이더 (기본 가중치 고정으로 대체) |
| 5 | Layer 2 ML 전체 |
| 6 | 조건 완화 추천 |

**끝까지 지키는 최소 집합**: 겨울 일조 · 조망 개방도 · 도로/철도 중심선 거리 · 추정 역 접근성 · **S1 전역 필터링** · D8 배포.

---

## 9. 리스크

| # | 리스크 | 영향 | 확률 | 대응 |
|---|---|---|---|---|
| **R2** | **단지↔동 매칭 실패** | **높음** | **중상** | D1 최대 이슈. 좌표 매칭 전환. 실패 단지는 **정렬에서 제외 + 회색 마커로 사유 표기**. `LOW` 신뢰도는 정렬 근거로 쓰지 않음 |
| **R2b** | D3 지연 → 전 일정 연쇄 | 높음 | 중상 | §8.2 즉시 발동 |
| ~~R11~~ | ~~학습 셀 커버리지 부족~~ | — | — | **D2에 해소.** 게이트를 "커버 단지 60%"에서 **"5건+ 셀의 거래 점유율 ≥ 80%"로 교체** — 전자는 거래 5건+ 단지가 57.9%뿐이라 어떤 셀 설계로도 달성 불가한 기준이었다. 실측 82.3% PASS |
| R12 | 실거래 API 일 호출 한도 | 중 | 중 | 학습 데이터는 `rt.molit.go.kr` CSV 벌크 병행 |
| R6 | "일조·조망은 이미 있다" 지적 | **높음** | **높음** | §3 탐색 vs 확인 프레임. 발표 첫 30초를 **필터링 데모**로 |
| R7 | "생성형 AI 없으면 AI냐" 지적 | 중 | 중 | 물리 시뮬 + LightGBM + SHAP = "환각 없는 검증 가능한 AI" |
| R8 | 카카오맵 도메인 미등록으로 프로덕션 지도 실패 | 중 | 중 | D8 배포 체크리스트 |
| R13 | 교통량 관측 지점 결측 | 낮음 | 높음 | 300m 내 지점 없으면 **결측 처리 + "관측 지점 없음" 표기.** 보간하지 않음 |
| ~~R1~~ | ~~horizon 배치 시간 초과~~ | — | — | **D1에 해소** (6분) |
| ~~R3~~ | ~~GIS건물통합정보 라이선스~~ | — | — | **D1에 해소** (OSM 전환) |

---

## 10. 검증 계획

| 대상 | 방법 | 기준 | 상태 |
|---|---|---|---|
| 자기 건물 제외 | 단독 배치 동의 일조 계산 | 거의 전 시간 확보 | ✅ D1 통과 |
| 높이 datum | 경사지에서 아래/위 동 차폐 비교 | 위쪽 동이 더 높게 계산 | ✅ D1 통과 |
| near-field 해상도 | 인접 동 10~20m 관측점 | 방위 bin 연속 차폐 | ✅ D1 통과 |
| 층↑ → 일조↑ | 층대별 중앙값 | 단조 증가 | ✅ D1 통과 |
| 단지 매칭 | 건축물대장 동명칭·층수·높이 대조 (전수) | 등급별 독립 검증 | ✅ D3 (HIGH 97.2% / NAME 94.4% / LOW 87.0%) |
| 단지 매칭 | 층화 정답셋 100개 육안 검수 | HIGH 무오류, MEDIUM 90%+ | 대장 전수 대조로 상당 부분 대체됨 |
| 조망 | 한강변 단지 `river_view` | True | D4 |
| 민감도 | 관측 높이 ±3m, 반경 ±500m | 순위 불변 | D4 |
| 좌표계 | 알려진 두 지점 거리 | 오차 1% 이내 | D4 |
| 시세 모델 | 시간 split + **단지 홀드아웃** | 물리 feature 유무 차이 보고 | D9 |
| 웹앱 | 실기기 전 화면 | 가로 스크롤 없음, 3초 내 로드 | D8 |

각 배치 스크립트는 **assert 기반 자체 검증**을 포함한다. 별도 테스트 프레임워크는 도입하지 않는다.

---

## 11. 스크립트

저장소는 디렉토리 수준으로 분리한다 — `data/`(배치) · `models/`(지표·ML) · `front/`(웹앱) · `docs/`.
번호 접두사는 실행 순서이며 폴더를 가로질러 이어진다. 실행은 항상 저장소 루트에서 한다.

```bash
.venv/bin/python -u data/master/15.assign_dong_all.py 2>&1 | tee output/15.run.log
```

### data/collect — 수집 (재실행 드묾)

| 파일 | 목적 | 상태 |
|---|---|---|
| `11.collect_trades.py` | 서울 25구 실거래 5년치 (재개 가능) | ✅ (매매 24.7만 / 전월세 130.7만) |
| `12.collect_osm.py` | 서울 전역 OSM 건물 + 단지 경계 + POI (타일 캐시) | ✅ (아파트 37,269동) |
| `19.collect_building_ledger.py` | 건축물대장 표제부 — 동명칭·층수·높이·세대수 | ✅ (주거동 22,925) |

**미수집 잔여**: 전월세 2건(종로구 202209, 중랑구 202606)이 connect timeout으로 빠져 있다.
API 한도 문제가 아니므로 `11`을 다시 돌리면 캐시를 건너뛰고 이 2건만 받는다.

### data/master — 마스터 구축 (재실행 잦음)

| 파일 | 목적 | 상태 |
|---|---|---|
| `14.geocode_all.py` | 매매 ∪ 전세 9,164단지 카카오 지오코딩 | ✅ (99.96%) |
| `15.assign_dong_all.py` | 동 → 단지 배정. 대장 동명칭 + 경계 폴리곤 + 등록 동수 상한 + 층수 등급화 | ✅ (동수 정확 93.8%, 과다 0건) |

### data/verify — 검증 (매번 실행)

| 파일 | 목적 | 상태 |
|---|---|---|
| `13.cell_coverage.py` | 학습 셀 커버리지 측정 (R11 근거) | ✅ (거래 점유율 82.3%) |
| `16.audit_sample.py` | 층화 검수 표본 100건 + 엑셀본 생성 | ✅ (검수 대기) |
| `17.verify_registry.py` | 한국부동산원 등록 정보 대조 | ✅ (주소 매칭 96.6%) |
| `20.verify_dong_names.py` | 건축물대장 동명칭 대조 | ✅ (독립 검증 90.3%) |
| `18.capture_audit.py` | 검수용 위성사진 캡처 | ⚠️ Esri 영상이 서울에서 낡아 신축 단지 판정 불가 |

### models/horizon — 지표 엔진

| 파일 | 목적 | 상태 |
|---|---|---|
| `05.horizon_prototype.py` | **horizon 엔진 + 기하 버그 검증 + 벤치마크** | ✅ D1 |
| `21.horizon_batch.py` | horizon 엔진 서울 전역 배치 (일조 3종 + 조망) | ✅ D4 (관측점 66,657 / 86초) |
| `22.access_metrics.py` | 역 접근성 + 생활 인프라 밀도 | ✅ D4 (9,160단지 / 0.6초) |
| `23.build_complex_metrics.py` | 최종 3테이블 조립 (`complex` / `complex_metrics` / `horizon_profile`) | ✅ D5 |

### data/archive — 강남구 프로토타입 (실행 안 함)

`docs/decisions.md`가 설계 근거로 인용하므로 삭제하지 않고 보존한다.

| 파일 | 목적 | 결과 |
|---|---|---|
| `01.chain_test.py` | 사슬 OSM 구간 1차 측정 | ✅ |
| `02.diagnose_chain.py` | 실패 원인 분해 (빌라 혼입 / 경계 / 보간) | ✅ |
| `03.cluster_complexes.py` | buffer-union 단지 복원 검증 | ✅ |
| `04.cluster_refine.py` | 준공년도 재분할 | ✅ |
| `06.probe_molit_api.py` | 실거래 API 키·스키마 검증 | ✅ |
| `07.link_chain.py` | 문자열 매칭으로 사슬 연결 | 32.5% (폐기) |
| `08.geocode_match.py` | 지오코딩 → 좌표 공간 매칭 | 96.9%이나 과병합 (폐기) |
| `09.assign_dong.py` | 동 → 단지 1:1 배정 (방향 전환) | 15의 원형 |

### docs/mockups

| 파일 | 목적 |
|---|---|
| `10.render_mockups.py` | HTML 목업 4장을 1200x800 PNG로 렌더 |

---

## 12. 실행 환경

- 프로젝트 경로: `/home/yjkim/test/imnjang` (로컬 확인 시 `Z:\home\yjkim	est\imnjang`)
- 가상환경은 이동 대상이 아니다. 서버에서 새로 만든다.

```bash
cd /home/yjkim/test/imnjang
python -m venv .venv
.venv/bin/pip install geopandas shapely pyproj rtree pvlib lightgbm requests pandas playwright
.venv/bin/playwright install chromium   # 목업 렌더링용
```

- `.env`에 API 키 3종(공공데이터포털, 카카오 REST/JS)이 있다. **서버에서 권한을 제한한다**:
  `chmod 600 /home/yjkim/test/imnjang/.env`
  (Z: 마운트에서는 chmod가 적용되지 않으므로 서버에서 직접 실행해야 한다)
- 모든 스크립트는 `Path(__file__).parent` 기준이라 경로 수정 없이 동작한다.
- `to_csv`에 `lineterminator="
"`을 고정했다. Windows에서 재실행해도 CRLF가 섞이지 않는다.
