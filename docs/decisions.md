# 계획 변경 이력

> v1 → v2 → v3 변경 근거와 외부 리뷰 대응 기록.

## 0. v2 → v3 변경 요약

2차 외부 리뷰에서 **v2의 horizon 엔진에 결과를 통째로 무효화하는 버그 3개**가 발견되어 수정했다.

| # | 발견된 버그 | 결과 | v3 수정 |
|---|---|---|---|
| 1 | **자기 건물을 차폐 후보에서 제외하지 않음** | 관측점이 동 중심이므로 자기 폴리곤이 전 방위를 가림 → **모든 단지 일조 0시간** | 후보 집합에서 자기 `building_id` 제외. §6.1 |
| 2 | **높이 기준(datum) 혼용** | 관측 높이는 해발(AMSL)인데 건물 높이는 지면 기준(AGL). 경사지에서 수십 m 오차 | 후보 건물 높이 = `해당 건물 지반고 + 층수×2.8m`. 양쪽 모두 AMSL로 통일 |
| 3 | **near-field densify 간격 부족** | 10m 거리 벽면에서 5m 간격 = **26.6° 공백**. 5° 해상도를 표방할 수 없음 | 거리 비례 adaptive densify. 50m 이내는 edge 샘플링 강화 |

추가 수정 및 개선:

| # | 항목 | 내용 |
|---|---|---|
| 4 | 사실과 다른 표현 정정 | "도로 경계" → **"도로 중심선"**(OSM은 centerline). "도보 10분" → **"추정 도보 10분"** |
| 5 | `river_view` 근거 강화 | "트인 방향에 한강이 있다"와 "한강이 보인다"는 다름. 실제 시선(LOS) 교차 판정을 별도로 수행 |
| 6 | `prediction` 스키마 불일치 수정 | §6.5 본문(면적×층)과 §7 스키마(floor_band)가 어긋나 있었음. **floor_band로 통일** |
| 7 | 매칭 정답셋 50 → **72개 이상 층화 표본** | 50개 전건 정확해도 95% 신뢰구간 하한이 92.9%라 "95% 정확도" 주장을 지지하지 못함 |
| 8 | **조건 완화 추천 기능 추가** (신규) | 조건 만족 단지가 0개일 때 "무엇을 최소로 포기하면 후보가 생기는지" 제시. §6.7 |
| 9 | D3 지연 시 선제 절단 목록 명시 | §10.3 |
| 10 | **ML 학습 데이터 확보 검증 추가** (§5.4) | v2까지 "API가 존재한다"만 확인했고 "11일 안에 받을 수 있는가"는 미검증이었음. **개발계정 일 한도(~1,000건)로 3,000회 호출은 3일 소요** → 학습 데이터는 `rt.molit.go.kr` CSV 벌크로 우회. 셀 커버리지 리스크(R11)도 신설 |
| 11 | **생활 인프라·유동 지표 추가** (§6.8) | 학교·병원·마트·백화점·편의점·공원·야간상권·역 승하차·교통량·건폐율/용적률. 전부 POI 거리 집계라 연산 비용 무시 수준 |

**2차 리뷰에서 반박하여 유지한 결정**

| 쟁점 | 리뷰 의견 | 유지 근거 |
|---|---|---|
| 서울 전역 커버리지 | 1차에서 "6~10 단지로 축소" → **2차에서 철회, 유지가 맞다고 인정** | §3.2 |
| 배치 6시간이 리스크 | 리스크로 지적 | 배치는 D4 저녁 착수 → D5 아침 수령. **사람이 기다리는 시간이 아니다** |
| 반경 1km 부족 | 물리적 horizon 경계가 아니라고 지적 | **일조에 대해서는 수치적으로 충분.** 서울 동지 남중고도 ≈29°, 1km 밖 건물이 29° 앙각을 만들려면 높이 554m 필요 → 롯데월드타워(555m) 하나뿐. 조망 개방도(10° 기준, 176m 이상)에만 부분 유효 → §6.1에 한계로 명시 |

---

## 0.1 v1 → v2 변경 요약 (이력)

외부 리뷰(codex)와 사실 검증 결과, v1의 두 가지 전제가 **틀린 것으로 확인**되어 계획을 수정했다.

| # | v1의 전제 | 검증 결과 | v2 대응 |
|---|---|---|---|
| 1 | "일조·조망 시뮬레이션은 아무도 안 한다" | **거짓.** 호갱노노·직방이 3D 단지투어로 계절별·시간대별 일조량과 세대별 조망을 이미 제공. 더스택은 "조망권·일조권 정량화"를 내걸고 서비스 중 | 일조·조망은 **기본 기능으로 유지**하되, 차별점 주장을 여기서 하지 않음. 차별점을 §3으로 이동 |
| 2 | "실거래 데이터로 향·층별 가격 기여도를 학습한다" | **불가능.** 공공 실거래 API는 층까지만 제공. **향(向)과 호수가 없고** `aptDong`도 결측이 흔함. 같은 동·같은 층에 남향/북향이 공존하므로 한 거래를 여러 feature 조합에 중복 배정하게 됨 | 세대 단위 SHAP 가격 기여도 **전면 폐기**. 가격 모델을 **단지 레벨**로 강등 |

추가로 반영한 개선:

| # | 개선 | 효과 |
|---|---|---|
| 3 | 일조 엔진과 조망 엔진을 **하나의 horizon profile 엔진으로 통합** | 연산량 6,960만 → 864만 광선. **8배 감소** |
| 4 | 광선-폴리곤 교차를 **꼭짓점 극좌표 변환 + 방위 binning**으로 대체 | 스칼라 루프 → numpy 벡터화. 추가로 수십 배 |
| 5 | 소음 "dB 추정" → **"도로 근접도" 사실 표기**로 강등 | 캘리브레이션 라벨 없이 절대 수치를 주장하지 않음 |
| 6 | 거래-단지 매칭을 `aptSeq` 기준으로 변경 | 단지명 fuzzy match 의존 제거 |
| 7 | 카카오 도보경로 API 배제 (일 1,000건 제한) | 4,000단지 접근성 계산 불가 → 직선거리 + 표고차로 대체 |

---

## D2 (09-10) 결정

| # | 결정 | 근거 |
|---|---|---|
| 11 | **면적타입 = 3m 폭 bin**으로 정의 | 문서에 정의가 없던 값. 1m 반올림은 84.5978/84.9984를 갈라놓고, 5m는 다른 평형을 섞는다 |
| 12 | **예측 단위 유지** (단지 × 면적타입 × 층대) | floor_band 제거 실익이 학습가능 셀 +9.8%p뿐. 층대는 일조·조망이 갈리는 축이라 빼면 물리 지표와 시세의 연결이 끊긴다 |
| 13 | **R11 게이트 기준 교체** — "커버 단지 60%" → "5건+ 셀의 거래 점유율 ≥ 80%" | 거래 5건+ 단지가 57.9%뿐이라 전자는 어떤 셀 설계로도 달성 불가한 기준이었다. 실측 82.3% |
| 14 | **높이 결측 4,541동은 같은 단지 동 중앙값으로 대체** | 단지 내 동 높이는 유사하다. 대체 여부를 플래그로 남겨 리포트에 공개하고, 매칭 실패로 대체 불가한 동만 제외 |
| 15 | **실패 로그의 serviceKey 마스킹 의무화** | `requests` 예외 메시지가 요청 URL을 통째로 담아 키가 평문으로 파일에 기록됐다 (D2에 실제 발생, 커밋 전 제거) |

---

## D3 (09-10) 결정

| # | 결정 | 근거 |
|---|---|---|
| 16 | **단지 모집단 = 매매 ∪ 전세** (9,164단지). 월세 전용 499단지 제외 | HANDOFF §6은 매매 7,883만 적었으나 plan.md §2 거래 유형이 "매매 + 전세"다. 전세로만 거래된 1,780단지를 버리면 전세 예측 대상이 사라진다 |
| 17 | **전월세 API의 `roadNm`은 건물번호를 포함한다** (`"경교장길 35"`). 매매는 도로명만 (`"경교장길"`) | 두 소스를 합쳐 mode를 뽑으면 거래량이 많은 전월세 형태가 이겨 `"경교장길 35 35"`가 만들어진다. 지오코딩 실패 797/2,179건이 여기서 나왔다. 도로명 끝 숫자 토큰을 제거한 뒤 본번을 붙인다 |
| 18 | **동 배정의 재배분 라운드 폐기** | 굶은 앵커가 여유 앵커에서 1동을 뺏어오는 방식. 커버리지 66.5% → 85.7%로 게이트는 통과했으나, 재배분으로만 동을 받은 1,425단지의 준공년도 일치율이 **37.1%** (1차 배정 단지는 88.7%), 배정 거리는 191m 대 71m였다. 커버리지 지표를 오답으로 채운 것 |
| 19 | **배정 근거를 거리에서 단지 경계 폴리곤으로 교체** (12.2, landuse 4,285개) | 최근접 원형 반경은 3ha+ 단지에서 앵커가 단지 중심에서 중앙값 112m 벗어나고 34.7%는 단지 반경 밖이라 구조적으로 틀린다. 경계 기반 단지의 준공년도 일치율 **99.5%**, 최근접 기반은 73.1% |
| 20 | **앵커→경계 스냅 허용치 = 0m** | 0/25/50/100/150m 스윕에서 경계 기반 일치율이 95.2 → 90.4 → 85.2 → 82.2 → 81.3%로 단조 감소하고 커버리지는 2.3%p만 늘었다. 앵커가 경계 밖이라는 것 자체가 다른 단지라는 신호다 |
| 21 | **준공년도를 배정에서 제거, 검증 전용으로 격리** | 초판은 준공년도가 맞는 앵커를 고른 뒤 준공년도로 검증해 `rank>=0` 동의 일치율이 정의상 100%였다. 게이트가 측정한 것은 정확도가 아니라 강제배정 비중이었다. 순환을 끊은 뒤 처음 측정된 실제 값이 82.6% |
| 22 | **배정 신뢰도 등급 부여** — HIGH(경계) / MIXED / LOW(최근접) | 근거가 다르면 정확도가 다르다(99.5% / 82.2% / 73.1%). 같은 값으로 쓰면 안 된다. plan.md §10 층화 검수의 층으로 그대로 사용한다 |
| 23 | **"동 중복 없음" 게이트 폐기** → "단지당 동 수 ≤ 100" | `assert osm_id.is_unique`는 12에서 이미 dedup한 입력 테이블의 행 유일성을 검사하는 것이라 알고리즘이 망가져도 항상 PASS였다. 실제 실패 양상은 독식(최대 257동)이므로 그것을 직접 잰다 |

### D3 미해결 (게이트 3개 FAIL 상태)

| 항목 | 실측 | 기준 | 비고 |
|---|---|---|---|
| 단지당 동 수 | 최대 257 | ≤ 100 | 전부 LOW 경로. 밀집 지역 독식 미해결 |
| 동을 받은 단지 | 64.6% | ≥ 80% | 거래량 가중 91.8%. HIGH만 20.3%(거래량 49.5%) |
| 준공년도 ±2년 | 82.6% | ≥ 90% | HIGH만 보면 99.5% PASS |

한계의 정체는 알고리즘이 아니라 **OSM landuse 폴리곤 커버리지**다. 4,285개로 서울 단지의 29%만 덮는다.

---

## D3 (09-10, 연장) 결정 — 건축물대장 도입 이후

| # | 결정 | 근거 |
|---|---|---|
| 24 | **한국부동산원 공동주택 단지 식별정보를 정답 동수로 채택** | 파일 CSV로 로그인 없이 전량 확보 가능(서울 아파트 9,458건). 지번주소 매칭률 96.6% |
| 25 | **건축물대장 표제부의 주거동 필터는 주용도가 아니라 `세대수 > 0`** | 주차장·주민운동시설까지 주용도가 '공동주택'으로 온다. 마곡동 744: 표제부 51건 → 세대수>0 19건, 세대 합계 1,529로 등록 정보와 정확히 일치 |
| 26 | **`bjdongCd`는 등록 정보의 필지고유번호(PNU)에서 추출** | 건축물대장 API에 법정동코드가 필수인데 실거래에는 없다. PNU 19자리 = 법정동코드(10)+필지구분(1)+본번(4)+부번(4). 별도 코드표 수집이 불필요해졌다 |
| 27 | **배정 근거 우선순위: 대장 동명칭 → 경계 폴리곤 → 최근접 거리** | 지번 안에서 동명칭은 사실상 유일(22,925건 중 중복 14건)하나 전역으로는 유일하지 않다(서울에 '101동' 1,845개). 공간으로 좁힌 뒤 이름으로 가르는 순서여야 한다 |
| 28 | **이름이 대장 목록에 없으면 그 쌍을 금지.** 단 대장이 등록 동수를 다 담은 단지에만 적용 | 여러 지번에 걸친 단지는 대장이 일부만 담아, 금지하면 자기 동까지 못 받는다 |
| 29 | **층수 불일치 쌍은 버리지 않고 최하 순위로 강등** (`LEVEL_MISMATCH` 등급) | 제거하면 정밀도는 오르나(독립 지표 82.0%→94.9%) 커버리지가 72.2%→65.2%로 빠진다. 강등하면 커버리지 1%p만 잃고 등급으로 정밀도를 회수한다 |
| 30 | **배정에는 층수만, 검증에는 높이만 쓴다** | 등록 동수와 동명칭을 배정에 쓴 뒤 그 값으로 검증하려다 두 번 순환에 빠졌다. 남은 독립 신호는 높이 하나뿐이므로 이것을 배정에 쓰면 검증 수단이 사라진다 |

### D3 최종 실측

배정 21,946동 / 37,269 · 커버리지 71.2% (거래량 가중 96.0%) · 등록 동수 정확 93.8% · 과다 배정 0건

**독립 검증 (높이 ±3m 일치, 높이는 배정에 미사용)**

| 등급 | 근거 | n | 일치율 |
|---|---|---|---|
| `HIGH` | 단지 경계 폴리곤 | 1,888 | 97.2% |
| `NAME` | 건축물대장 동명칭 | 9,505 | 94.4% |
| `LOW` | 최근접 거리 | 3,217 | 87.0% |
| `LEVEL_MISMATCH` | 층수가 대장과 불일치 | 469 | **1.5%** |

층수 불일치는 오배정의 거의 확정적인 지표다. 이 469동을 제외하면 나머지는 90% 이상 신뢰할 수 있다.

### 폐기된 접근 (반복하지 말 것)

| 접근 | 실패 이유 |
|---|---|
| 재배분 (굶은 앵커가 여유 앵커에서 1동 회수) | 붙은 동의 63%가 준공년도 불일치, 배정 거리 3.4배 |
| 앵커를 경계 밖으로 스냅 | 0/25/50/100/150m 스윕에서 정확도 단조 감소 |
| 준공년도를 배정 기준으로 사용 | 그 값으로 다시 검증해 순환 지표가 됨 |
| Esri 위성사진 육안 검증 | 서울 영상이 낡음. 2015년 준공 단지 위치에 재개발 이전 주택가가 찍힘 |

### 검증 스크립트에서 잡은 자체 버그 3건

모두 "결측값을 유효값으로 취급"하는 같은 계열이다. 검증 코드가 틀리면 잘못된 결론이 그대로 결정이 된다.

| 버그 | 증상 | 잘못된 측정값 |
|---|---|---|
| `name is not None`이 NaN을 통과 | 이름 없는 동 하나가 단지 전체를 불일치로 뒤집음 | 51.1% → 23.2% |
| `Series.get()`이 NaN 반환 | 층수 대조 분기가 통째로 죽어 `FLOOR_*` 판정 0건 | 판정 누락 |
| 단독동 단지의 빈 동명칭을 집합에 포함 | 이름 집합이 `{''}`가 되어 무엇이든 불일치 | 97.5% → 67.7% |

---

## D4 (09-10, 연속 작업) 결정

| # | 결정 | 근거 |
|---|---|---|
| 31 | **차폐물은 `building` 태그와 무관하게 height가 있는 모든 건물** (48,211동) | 아파트가 아닌 건물도 그림자를 만든다. 배정 여부와 차폐 여부는 별개다 |
| 32 | **05의 함수를 `importlib`으로 로드해 재사용** | 파일명이 숫자로 시작해 일반 import가 문법상 불가능하다. 알고리즘을 복붙하면 D1 검증을 통과한 구현과 갈라진다 |
| 33 | **멀티프로세싱 도입** (fork, 14 워커) | 단일 프로세스 706초로 10분 상한 초과. 서울 전역 차폐 후보 밀도가 강남 프로토타입의 3배(관측점당 8.9ms vs 2.9ms)였다. 건물별 계산이 독립이므로 알고리즘 변경 없이 병렬 실행만 추가. 병렬 전후 지표 동일 확인 |
| 34 | **지면을 평지(z=0)로 가정** — 알려진 한계로 공개 | DEM이 없다. 05는 합성 데이터로 AMSL datum을 검증했으나 실데이터에 표고 컬럼이 없다. 경사지에서 오차가 생기며 DEM 확보 시 교체 대상 |
| 35 | **층대 분모를 건축물대장 지상층수로 통일** | 13은 최고 거래층, 21은 OSM levels를 분모로 써 27.6%의 단지에서 3층 이상 어긋났다. 예측 단위가 단지×면적타입×층대인데 학습 셀과 지표의 "MID"가 다른 층을 가리키면 모델이 잘못 학습된다. 대장 층수는 OSM levels와 ±1층 84.1%, 최고 거래층과는 70.3% 일치 |

### terra(코드 검증)가 찾은 치명 버그 3건

| 버그 | 증상 | 수정 후 |
|---|---|---|
| `registry.drop_duplicates("join_key")` | 한 지번의 여러 공식 단지가 첫 행 하나로 뭉개짐. 압구정동 456의 현대6차(8동)·현대7차(7동)가 현대65동의 1동을 상한으로 받음 | 단지명 유사도로 1:1 매칭. 49개 지번 118행 영향 |
| `ceil(reg_dong / share)` | capacity 합계가 등록 동수를 초과. aptSeq 단위로만 검사해 "과다 0건"으로 숨김 | 최대잔여법으로 정수 배분. 게이트를 지번 단위 검사로 교체 (7건 → 1건) |
| 건축물대장 API 페이지네이션 누락 | `numOfRows=100, pageNo=1`만 요청. 헬리오시티가 84동 중 52동만 수집됨 | `totalCount` 확인 후 이어받기. 5개 지번 재수집, 전부 100건 초과였음 |

**효과**: 등록 동수 정확 일치율 93.8% → **95.6%**, 헬리오시티 52동 → 84동(등록과 정확히 일치).

이 버그들은 모두 **검증 지표를 좋아 보이게 만드는 방향**으로 작동하고 있었다. 특히 두 번째는 게이트 자체가 초과를 못 보게 설계돼 있었다.

### 독립 검증 지표의 재정의 (sol 검토 반영)

높이(`height_m`)를 독립 검증 지표로 내세웠으나 **틀렸다**. sol 검토에서 층수와 높이의
상관이 OSM 직접 태그 기준 r²=0.93, 대장 기준 r²=0.65로 확인됐다. 층수로 거른 뒤
높이로 검증하는 것은 사실상 같은 변수를 두 번 쓰는 것이다.

**준공년도가 유일한 완전 독립 신호다.** D3에서 순환성 때문에 배정에서 제거했고,
그 뒤로 어떤 배정 근거에도 쓰이지 않았다. 층수·높이와도 물리적으로 무관하다.

| 등급 | n | 준공년도 ±2년 일치 | 중앙값 차 | (참고) 높이 ±3m |
|---|---|---|---|---|
| `HIGH` (경계 폴리곤) | 3,043 | 99.1% | 0년 | 97.2% |
| `NAME` (대장 동명칭) | 12,034 | 94.6% | 0년 | 94.4% |
| `LOW` (최근접 거리) | 3,859 | 84.4% | 0년 | 87.0% |
| `LEVEL_MISMATCH` | 474 | **19.0%** | **12년** | 1.5% |

등급 순서가 두 지표에서 동일하게 재현되므로 **등급 체계 자체는 유효**하다.
다만 앞으로 정확도를 주장할 때는 **준공년도 수치를 쓴다**. 높이 수치는 층수와의
상관 때문에 부풀려져 있다.

이로써 이 프로젝트에서 배정 근거로 쓴 신호와 검증에 남긴 신호가 정리된다:

| 신호 | 배정 사용 | 검증 사용 | 비고 |
|---|---|---|---|
| 등록 동수 | ✅ 상한 | ❌ | 순환 |
| 대장 동명칭 | ✅ 0순위 | ❌ | 순환 |
| 대장 지상층수 | ✅ 등급화 | ❌ | 순환 |
| 대장 높이 | ❌ | △ | 층수와 r²=0.93. 보조 지표로만 |
| **준공년도** | ❌ | ✅ | **유일한 완전 독립 지표. 배정에 쓰지 말 것** |

| # | 결정 | 근거 |
|---|---|---|
| 36 | **일조 지표를 3종으로 확장** — 09~15 총합(기존) / 09~15 연속 최대 / 08~16 총합 | 기존 구현은 09~15 창을 쓰면서 총합을 재 법정 기준 어느 쪽도 아니었다. 일조권 수인한도는 동지일 기준 "09:00~15:00 중 연속 2시간" 또는 "08:00~16:00 중 총 4시간" 둘 중 하나다 |
| 37 | **정렬 기본축은 `winter_total_hours_8_16`** | 상한이 8.08h로 넓어 고유값 902 → 1,160, 상위 500위 동점 500 → 272로 완화. 세 지표 중 유일하게 변별력이 개선됐다 |
| 38 | **일조 포화는 지표 결함이 아니라 물리 현상. 해법은 다축 정렬** | 차폐가 없는 단지는 같은 위도에서 같은 태양을 보므로 **실제로 동등**하다. 어떤 일조 지표를 써도 상단은 포화한다. 실측: 8~16시 만점 272단지의 `open_angle_mean`이 256개 고유값(72~176도)으로 완전히 갈린다. 정렬은 일조 → 조망 개방도 → 접근성 순의 다축이어야 한다 |

### 폐기 검토: `winter_continuous_hours`

법정 기준 1에 정확히 대응하므로 **표기용으로는 유지**하되 **정렬 축으로는 쓰지 않는다.**
실측 결과 `winter_sunlight_hours`와 만점 비율·동점 개수가 완전히 동일했다. 건물 차폐가
태양 방위각 스윕에서 대체로 단일 연속 구간으로 나타나(여러 건물이 서로 다른 방위에서
따로 가리는 경우가 드묾) 총합과 최장연속이 같아지기 때문이다.

| # | 결정 | 근거 |
|---|---|---|
| 39 | **커버리지 71.0%를 PASS로 승인하지 않고, 게이트를 거래량 가중으로 바꾸지도 않는다** | 거래량 가중 96.0%는 "최근 거래 활동의 대부분을 커버한다"는 보조 KPI로는 유효하나, 저거래·신규·거래가 드문 고가 단지를 구조적으로 낮게 평가한다. R11이 거래량 가중을 쓴 것은 학습 데이터의 목표 단위가 거래였기 때문이며 단지 탐색 게이트와는 목적이 다르다 |
| 40 | **웹 개발과 커버리지 게이트를 분리해 병행한다** | 6,450단지로 기능 구현과 데모가 가능하다. `plan.md` §8.1의 "5개 구 축소"는 `complex_metrics`가 **아예 없을 때만** 발동하므로 해당 없다 |
| 41 | **`residential`/`yes` 태그를 일괄 허용하지 않는다.** 대장 동명칭·층수·등록 동수·준공년도 검증을 통과한 후보만 단계적으로 채택 | 일괄 확장은 재배분(커버리지 85.7%, 준공년도 일치율 37.1%로 폐기)과 같은 위험이다. 실측상 미배정 앵커 중 landuse 경계 안에 residential/yes 후보가 있는 경우는 1건뿐이고, 합집합 후보 1,457개 중 준공년도 ±2년이 맞는 것은 280개에 불과하다 |
| 42 | **앱에서 미배정 단지를 필터 탈락처럼 숨기지 않는다.** `분석 불가 — 건물 footprint 미매칭`으로 구분 표기하고 전체·구별 커버리지를 노출 | `plan.md` §8.1의 "조용히 줄이지 않는다" 원칙. 동시에 §3이 전 단지 사전 계산을 차별점의 전제로 두므로 현 상태를 "서울 전역 완전 검색"으로 표현해서는 안 된다 |
| 43 | **층수 4순위 폴백: 높이 ÷ 층고(2.8m)로 역산** | 올림픽파크포레온(둔촌주공 재건축, 거래 1,257건)이 47동 전부 levels 결측이라 지표에서 통째로 빠져 있었다. 높이는 전부 있어 역산으로 복구했다. 지표 없는 단지의 거래 영향 3,323건 → 1,461건 |

### 커버리지 개선 우선순위 (sol 제안)

일괄 확장 대신 다음 순서로 검증된 후보만 채택한다.

| 순위 | 대상 | 규모 |
|---|---|---|
| 1 | 미배정 중 거래 50건 이상 | 136단지 |
| 2 | 미배정 중 300세대 이상 | 32단지 |
| 3 | 배정됐으나 지표가 없는 단지 | 57단지 (거래 1,461건) |

3순위 중 상당수는 층수·높이가 모두 없어 복구 불가다(래미안라그란데 32동 등).

| # | 결정 | 근거 |
|---|---|---|
| 44 | **교통량 지표 폐기** | 서울시 교통량 관측 지점이 전역에 139개뿐이다. `plan.md` R13이 규정한 300m 기준으로 단지 커버리지가 **4.6%**(424/9,160), 중앙값 거리 1,071m다. R13은 "300m 내 지점 없으면 결측 처리, 보간하지 않음"을 못 박고 있으므로 95.4%가 결측인 컬럼이 된다. 필터로도 정렬로도 쓸 수 없다 |
| 45 | **소음원 근접도는 `road_centerline_m`로 대체** | 100% 채움(중앙값 111m)이고 `algorithms.md` §6.3이 애초에 dB 추정을 폐기하고 "중심선까지 N m" 사실 표기만 허용한다. 교통량보다 커버리지가 20배 넓다 |

### 서울 열린데이터광장 API 조사 기록 (재조사 방지)

| 서비스 | 결과 |
|---|---|
| `CardSubwayStatsNew` | 정상. 다만 승하차는 공공데이터포털 파일로 이미 확보해 불필요 |
| `SpotInfo` | 정상. 교통량 관측 지점 139개 (`spot_num`, `spot_nm`, `grs80tm_x/y` = EPSG:5181) |
| `VolInfo` | **ERROR-301 지속.** 키는 유효하고(다른 서비스 정상) 서비스명이 다르거나 폐기된 것으로 보인다. XML 전용 서비스라 `json` 요청 시에도 같은 오류가 난다 |

교통량을 폐기했으므로 `VolInfo` 서비스명 추적은 중단한다.

---

## 2026-09-11 ~ 09-12 — 정체성 재정립과 모델 결함 수정

| # | 결정 | 근거 |
|---|---|---|
| 46 | 제품 정의를 **"살기 좋은 순서로 정렬하는 지도" → "고른 단지를 같은 기준으로 비교"** 로 바꾼다 | 무엇이 좋은 집인지는 사람마다 다르다. 정렬 순서로 단정하면 반박 가능한 주관이 된다. 사용자가 고른 대상을 같은 자로 재는 것은 객관적이고 방어된다 |
| 47 | **"임장 대체" 포지셔닝을 버린다.** "임장 전 비교"로 낮춘다 | 향·호수·소음·실내 상태·실제 보행 동선·매물 가용성을 모른다. 대체한다고 말하는 순간 거짓이 된다 |
| 48 | 학습·검증은 **최근 24개월**만 쓴다 (화면 그래프는 5년 전체) | 지표가 2026년 현재 상태라 2021년 거래에 붙이면 그 사이 개통한 역·생긴 상권이 과거로 소급된다 |
| 49 | 가격 화면은 **baseline-first**. 모델은 단지 이력이 전무한 cold-start에만 쓴다 | 30.3에서 동일 셀 직전 거래 MAPE 7.95% vs M3 18.46%다. 단지 이력 자체가 없는 4.52% 구간에서는 M3 21.16%가 baseline 64.65%보다 낮다 |
| 50 | **SHAP 폐기** | 입지 feature 상관이 높아 attribution이 배경 설정에 좌우되고 인과를 뜻하지 않는다 |
| 51 | "실거주 적합도 종합 점수" 폐기 | 종합 판정은 주관이다. 같은 축에 나란히 놓는 데까지만 한다 |
| 52 | cold-start에 **전세를 쓴다** (면적타입 발견 + 가격 feature) | 매매 0건 단지 2,318개 중 1,290개에 전세 거래가 있다. 추정 가능 단지 1,025→2,315단지, 단지 holdout MAPE 12.65%→9.73% |
| 53 | 반전세는 **연 5% 전환율**로 환산해 쓴다 | K(순수 전세) 9.72% vs L(환산) 9.59%. 차이는 작으나 표본이 늘어 채택. 전환율은 고정 가정이며 실제가 4%면 −12%, 6%면 +10% 오차 |
| 54 | **임대 전용 단지 109개를 추정 대상에서 제외** | 매매가 구조적으로 발생하지 않는다. cold-start에 109개, 거래 있는 단지에 0개로 완벽히 갈린다. 살 수 없는 집에 가격을 매기면 오정보다 |
| 55 | 예측구간은 **시간×단지 이중 분할 conformal** | 단지만 나누면 시점 이동 오차를 담지 못해 test coverage가 70.92%로 떨어진다. 이중 분할로 80.13% |
| 56 | **지역 FE 결함 수정 — 이번 세션 최대 발견** | `bjd_code`가 법정동 코드 10자리 중 5자리만 담아 고유값 68개, 그중 45개가 자치구를 섞었다. 역삼동과 미아동이 같은 지역 범주였다. 자치구 결합 완전 키(335개)로 교체 |
| 57 | **일조·조망을 가격 모델에서 뺀다.** 환경 비교 전용 | 56 수정 후 재측정한 M2 vs M3 out-of-time MAPE 차이 bootstrap 95% CI는 [-0.039, +0.105]%p로 0을 포함한다. 이 사양·이 평가기간에서 물리 feature의 추가 예측 개선을 확인하지 못했다 |
| 58 | "한강 조망 +30.9%"와 "물리 ΔR² +0.079" 주장 **폐기** | 결함 상태의 값이었다. 현재 train M3 사양에서 `river_view`의 조건부 연관은 +6.0276% [3.0331, 9.1091]이고, 물리 전체 ΔR²는 +0.0021이다 |
| 59 | 알고리즘은 **LightGBM 유지** | 6종 비교에서 부스팅 3종이 0.26%p 안에 동률. 학습 6배 빠르고 conformal이 이미 맞춰져 있다. 노이즈 수준 차이로 바꾸지 않는다 |
| 60 | 토지거래허가구역은 **표시만, 모델 feature 금지** | 2025-10-20 국토부 지정으로 서울 전역 아파트가 허가구역이라 변별력 0. 다만 실거주 의무가 매수 판단에 직결되므로 표시는 필수 |
| 61 | 정적 payload는 **gitignore**, 배포 직전 1회만 커밋 | 55.9MB / 9,160개 파일이 재빌드마다 전량 바뀐다 |
| 62 | 표기 규칙은 **스크립트로 강제** (`front/build/37.check_wording.py`) | 표기 위반은 기능 결함이 아니라 사실 왜곡이다. 사람 눈에 맡기면 언젠가 새어 나간다 |
| 63 | **서비스 가격 셀 route rolling OOT를 도입** | 2026-02~07의 각 origin에서 다음 달 거래를 평가했다. score 가능한 전체 transaction-weighted MAPE는 7.75%, `CELL_LAST` route는 대상 89.2%, MAPE 7.48%다. `MODEL_REQUIRED`와 `NO_CELL_CANDIDATE`는 coverage에는 포함하되 MAPE denominator에서는 제외한다 |
| 64 | **37 알고리즘 비교 사양을 33 채택 사양에 정렬** | 37은 전세 variant·자치구 교호항을 33.2의 채택 사양에서 읽는다. 별도 사양으로 성능을 인용하지 않아 서비스 모델과 비교 대상이 갈리는 것을 막는다 |
| 65 | **적대적 리뷰 3차의 P0는 없음으로 판정** | 서비스 route rolling OOT, 37 사양 정렬, 문서의 수치·표기 정합을 반영했다. 남은 cold-start rolling-origin 첫거래 평가와 전세 trim as-of 문제는 공개 한계로 유지한다 |

### 아직 해결하지 못한 것 (적대적 리뷰 지적)

`product-identity.md` §9에 전부 적었다. 요약하면:

- 단지 holdout은 가격 정제 후 분할됐으므로 평가셋 16행(8단지, 0.075%)이 정제의 영향을 받았다
- 서비스 셀 rolling OOT는 도입됐으나 cold-start rolling-origin 첫거래 평가는 미실시다
- conformal이 엄밀하지 않다. 첫 coverage 실패를 보고 방식을 바꿨으므로 test가 재사용됐다
- 예측구간이 예측값의 0.88~1.25배로 너무 넓어 단지 간 우열 판정에 못 쓴다
- 전세 갱신계약을 구분하지 못한다. 중앙값은 방어 장치가 아니다
